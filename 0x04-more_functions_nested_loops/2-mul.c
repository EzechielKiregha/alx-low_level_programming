#include "main.h"
#include <stdio.h>

/**
 * mul - we multiply two numbers
 * @a: is int, the  multiplicant
 * @b: is int, the multiplier
 * Return: the product of a and b.
 */

int mul(int a, int b)
{
	int result;

	result = a * b;
	return (result);
}
