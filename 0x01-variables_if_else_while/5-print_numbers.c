#include <stdio.h>
/**
 * main -We print all digit to the std output.
 * using for loop through 0 - 10
 *
 * Return: 0
 *
 */

int main(void)
{
	int i;

	for (i = 0; i < 10; i++)
		printf("%d", i);
	printf("\n");
	return (0);
}
